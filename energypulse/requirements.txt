# ── Data Fetching ──────────────────────────────────────────────
entsoe-py==0.6.5              # Official ENTSO-E API client for European energy data
requests==2.32.3
python-dotenv==1.0.1          # Load .env API keys

# ── Data Processing ────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.0           # Preprocessing, metrics
holidays==0.46                # German public holidays as features

# ── Model ──────────────────────────────────────────────────────
lightgbm==4.3.0               # Gradient boosting — fast, accurate for tabular/time-series
optuna==3.6.1                 # Hyperparameter tuning

# ── MLflow ─────────────────────────────────────────────────────
mlflow==2.13.0                # Experiment tracking + model registry

# ── DVC ────────────────────────────────────────────────────────
dvc==3.51.2                   # Data versioning + pipeline reproducibility

# ── API ────────────────────────────────────────────────────────
fastapi==0.111.0              # REST API framework
uvicorn==0.30.1               # ASGI server to run FastAPI
pydantic==2.7.4               # Request/response validation

# ── Monitoring ─────────────────────────────────────────────────
evidently==0.4.30             # Data drift + model performance monitoring

# ── Scheduling ─────────────────────────────────────────────────
prefect==2.19.7               # Pipeline scheduling + orchestration

# ── Testing ────────────────────────────────────────────────────
pytest==8.2.2
pytest-cov==5.0.0             # Test coverage reports
httpx==0.27.2                 # For testing FastAPI endpoints

# ── Visualization ──────────────────────────────────────────────
plotly==5.22.0
matplotlib==3.9.0
